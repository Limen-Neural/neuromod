# Azure Pipelines for cross-platform parity (Linux/Windows/macOS)
# Runs on PRs and main. Uses Microsoft-hosted agents.
trigger:
  branches:
    include:
      - main
pr:
  branches:
    include:
      - main
variables:
  CARGO_HOME: $(Pipeline.workspace)/.cargo
jobs:
- job: BuildTest
  strategy:
    matrix:
      linux:
        imageName: 'ubuntu-latest'
      windows:
        imageName: 'windows-latest'
      mac:
        imageName: 'macos-latest'
  pool:
    vmImage: $(imageName)

  steps:
  - checkout: self
    persistCredentials: 'false'

  - task: Cache@2
    displayName: 'Rust Cache'
    inputs:
      key: 'cargo | "$(Agent.OS)" | Cargo.lock'
      restoreKeys: |
        cargo | "$(Agent.OS)"
      path: |
        $(Build.SourcesDirectory)/target
        $(CARGO_HOME)/registry
        $(CARGO_HOME)/git

  - bash: |
      curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
      echo "##vso[task.prependpath]$HOME/.cargo/bin"
    displayName: 'Install Rust (rustup) - Unix'
    condition: ne(variables['Agent.OS'], 'Windows_NT')

  - script: |
      curl -sSfL -o rustup-init.exe https://win.rustup.rs/
      rustup-init.exe -y --default-toolchain stable
      echo ##vso[task.prependpath]%USERPROFILE%\.cargo\bin
    displayName: 'Install Rust (rustup) - Windows'
    condition: eq(variables['Agent.OS'], 'Windows_NT')

  - script: rustup component add rustfmt clippy
    displayName: 'Install rustfmt + clippy'

  - script: cargo fmt -- --check
    displayName: 'Check formatting'

  - script: cargo clippy --all-targets --all-features -- -D warnings
    displayName: 'Clippy'

  - script: cargo build --all-features
    displayName: 'Build'

  - bash: |
      curl -LsSf https://get.nexte.st/latest/linux | tar zxf - -C ${CARGO_HOME:-~/.cargo}/bin
    displayName: 'Install nextest - Unix'
    condition: ne(variables['Agent.OS'], 'Windows_NT')

  - script: |
      curl -LsSf https://get.nexte.st/latest/windows-tar -o nextest.tar.gz
      tar zxf nextest.tar.gz -C %USERPROFILE%\.cargo\bin
    displayName: 'Install nextest - Windows'
    condition: eq(variables['Agent.OS'], 'Windows_NT')

  - script: cargo nextest run --all-features
    displayName: 'Test'

  - task: PublishTestResults@2
    displayName: 'Publish Test Results'
    inputs:
      testResultsFormat: 'JUnit'
      testResultsFiles: '**/junit.xml'
      mergeTestResults: true
      testRunTitle: 'Azure Tests'
    condition: succeededOrFailed()

  - script: |
      cargo install cargo-llvm-cov@0.6.10
      cargo llvm-cov --all-features --lcov --output-path lcov.info
    displayName: 'Coverage (optional)'
    continueOnError: "true"
    condition: eq(variables['imageName'], 'ubuntu-latest')
<<<<<<< HEAD

  - task: PublishCodeCoverageResults@2
    displayName: 'Publish Coverage'
    inputs:
      codeCoverageTool: 'LCOV'
      summaryFileLocation: 'lcov.info'
    condition: and(succeededOrFailed(), eq(variables['Agent.OS'], 'Linux'))
=======
>>>>>>> dc9d412 (fix(sentry): In order to prevent data races I added env_lock() so that the tests wait till it's their turn. All SENTRY_DSN environment varibles now follow strict static mutex. fix(azure): Remove the specific paths for Linux container in place for a more universial cache path fully cross-platform. fix(Dockerfile): Long-term maintainability was ensure by ditching the older package version.)
